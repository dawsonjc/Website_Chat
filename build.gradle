plugins {
    id "java"
    id "war"
}

dependencies {}

tasks.register("runApp") {
    dependsOn(":frontend:bootRun")
}

tasks.register("runAuth") {
    dependsOn(":auth_service:kt_service:kobwebStart")
}